spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: account-service
  cloud:
    config:
      uri: http://127.0.0.1:8890
      fail-fast: true
      password: root
      username: user

