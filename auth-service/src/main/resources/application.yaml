server:
  port : 8085

spring:
  application:
    name: auth-service

  data:
    mongo:
      repositories:
        type: reactive
    mongodb:
      authSource: admin
      database: auth
      user: root
      password: root
      host: localhost
      port: 27018

debug: false
