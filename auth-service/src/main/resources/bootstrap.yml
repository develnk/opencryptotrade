spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config:8890
      fail-fast: true
      password: root
      username: user
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  main:
    allow-bean-definition-overriding: true