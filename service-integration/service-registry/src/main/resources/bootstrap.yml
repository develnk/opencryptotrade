spring:
  application:
    name: service-registry
  cloud:
    config:
      uri: http://config:8890
      fail-fast: true
      password: root
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
